# 核心依赖（锁定稳定版，全部有预编译wheel，秒装）
streamlit==1.32.2
pandas==2.2.1
numpy==1.26.4
# Excel读写（财务项目必备，避免源码编译）
openpyxl==3.1.2
xlrd==2.0.1
# 数据可视化（财务分析用）
plotly==5.22.0
matplotlib==3.8.4
# 图像处理（streamlit依赖，指定预编译版）
Pillow==10.2.0
# 工具类
python-dotenv==1.0.1
